# Python Plugin

Generates Python test files for student exams.

## Status

Minimal implementation. Single exercise (`file_72.py`) as proof of concept.

## Key Files

- `index.js` — PythonPlugin class
- `Makefile.mk` — build target
- `utils.py` — test utilities
